@model Nop.Plugin.Widgets.MySlider.Areas.Admin.Models.MySliderModel

@{
    //page title
    ViewBag.Title = T("Nop.MySlider.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("MySlider");
}

<form asp-controller="MySlider" asp-action="Create" method="post" id="anywhere-slider-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Nop.MySlider.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Nop.MySlider.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>